{
  "name": "reviewlens-extension-e2e",
  "private": true,
  "version": "1.0.0",
  "description": "Playwright e2e tests for the ReviewLens Chrome extension: scrape -> analyze -> render.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0"
  }
}
